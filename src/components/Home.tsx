import React from 'react';
import styled from 'styled-components'

const HomeContainer = styled.section`
  padding: 3rem;
`

const Home: React.FC = () => {

  return (
    <HomeContainer>Home Page</HomeContainer>
  )
};

export default Home;